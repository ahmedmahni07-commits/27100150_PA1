# Task 2 — Unsupervised Domain Adaptation

TODO: fill in after implementation —
- exact PACS download source/path used
- attribution for any reused MMD/GRL/CDAN public implementation snippets
- final hyperparameters used for the main comparison (lambda_MMD=1, DANN/CDAN
  GRL schedule) and for the controlled design study
- how to reproduce: `python task2/train.py --config task2/configs/<method>.yaml`
  for each of source_only/dan/dann/cdan, then `python task2/evaluate_final.py`